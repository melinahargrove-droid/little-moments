const DRIVE_STATE_KEY='lm-google-drive-backup-v1';
const TOKEN_KEY='lm-google-drive-session-token-v1';
const LEDGER_KEY='lm-drive-protection-ledger-v1';
const app=document.querySelector('#app');
let startupTimer=null;

function readJSON(key){try{return JSON.parse(localStorage.getItem(key)||'{}')}catch{return {}}}
function writeJSON(key,value){try{localStorage.setItem(key,JSON.stringify(value))}catch{}}
function driveState(){return readJSON(DRIVE_STATE_KEY)}
function ledger(){return readJSON(LEDGER_KEY)}
function hasActiveToken(){try{const s=JSON.parse(sessionStorage.getItem(TOKEN_KEY)||'{}');return !!(s.token&&Number(s.expiresAt)>Date.now()+30000)}catch{return false}}
function when(v){if(!v)return'never';try{return new Date(v).toLocaleString(undefined,{month:'short',day:'numeric',hour:'numeric',minute:'2-digit'})}catch{return String(v)}}
function setDirty(detail={}){if(detail?.operation==='atomic-restore')return;const now=new Date().toISOString(),old=ledger();writeJSON(LEDGER_KEY,{...old,dirty:true,dirtyAt:now,lastChangeOperation:detail?.operation||'change',lastChangeStore:detail?.store||null});render()}
function confirmProtected(detail={}){const old=ledger(),backupAt=detail?.createdAt||new Date().toISOString();if(old.dirtyAt&&new Date(backupAt).getTime()<new Date(old.dirtyAt).getTime()){render();return}writeJSON(LEDGER_KEY,{...old,dirty:false,lastProtectedAt:backupAt,lastProtectedCounts:detail?.counts||old.lastProtectedCounts||null});render()}
function protectionStatus(){const d=driveState(),l=ledger(),token=hasActiveToken();if(!d.connected)return{kind:'off',title:'Drive not connected',detail:'Classroom changes are only on this device.',button:'Connect'};if(l.dirty&&!token)return{kind:'danger',title:'Changes not backed up',detail:'Reconnect Drive to protect your latest changes.',button:'Reconnect'};if(l.dirty&&token)return{kind:'working',title:'Protecting changes…',detail:'Uploading the newest classroom copy.',button:null};if(!token)return{kind:'warn',title:'Drive paused',detail:`Last backup ${when(d.lastBackupAt||l.lastProtectedAt)}`,button:'Reconnect'};return{kind:'ok',title:'☁ Protected ✓',detail:`Last backup ${when(d.lastBackupAt||l.lastProtectedAt)}`,button:null}}
async function reconnectAndProtect(btn){if(typeof window.__lmDriveBackupNow!=='function'){alert('Drive protection is still loading. Try again in a moment.');return}btn.disabled=true;btn.textContent='Connecting…';try{await window.__lmDriveBackupNow('reliability-protect',true);render()}catch(e){console.error(e);btn.disabled=false;btn.textContent='Try Again';alert(`Google Drive protection did not finish: ${e?.message||e}`)}}
function openSettings(){window.dispatchEvent(new Event('lm:open-settings'))}
function removeUI(){document.querySelector('#lm-drive-protection-bar')?.remove();document.querySelector('#lm-drive-protection-pill')?.remove()}
function render(){const warning=document.querySelector('.lm-data-warning'),screen=app?.querySelector('.screen');if(!screen||warning){removeUI();return}const s=protectionStatus();let bar=document.querySelector('#lm-drive-protection-bar'),pill=document.querySelector('#lm-drive-protection-pill');if(s.kind==='ok'){bar?.remove();if(!pill){pill=document.createElement('button');pill.type='button';pill.id='lm-drive-protection-pill';document.body.appendChild(pill)}pill.textContent=s.title;Object.assign(pill.style,{position:'fixed',right:'10px',top:'calc(8px + env(safe-area-inset-top))',zIndex:'9997',border:'1px solid #b8c9ae',background:'rgba(247,251,244,.94)',color:'#50664e',borderRadius:'999px',padding:'5px 8px',font:'800 10px system-ui,sans-serif',boxShadow:'0 2px 7px rgba(45,38,31,.08)',opacity:'.88'});pill.onclick=openSettings;return}pill?.remove();if(!bar){bar=document.createElement('aside');bar.id='lm-drive-protection-bar';document.body.appendChild(bar)}const palette=s.kind==='danger'?['#fff1ed','#a94f42','#d98a79']:s.kind==='warn'?['#fff8e7','#7d6732','#dfc878']:s.kind==='working'?['#f4f1e9','#665d51','#c9bba7']:['#fff3ed','#8b564e','#d9b2aa'];Object.assign(bar.style,{position:'fixed',left:'12px',right:'12px',top:'calc(8px + env(safe-area-inset-top))',bottom:'auto',zIndex:'9998',display:'grid',gridTemplateColumns:s.button?'1fr auto':'1fr',alignItems:'center',gap:'10px',padding:'9px 11px',borderRadius:'14px',background:palette[0],border:`1px solid ${palette[2]}`,boxShadow:'0 6px 18px rgba(45,38,31,.14)',font:'11px/1.25 system-ui,sans-serif',color:palette[1],maxWidth:'640px',margin:'0 auto'});bar.innerHTML=`<div><strong style="display:block;font-size:12px">${s.title}</strong><span style="display:block;margin-top:1px">${s.detail}</span></div>${s.button?`<button type="button" id="lm-drive-protect-action" style="border:1px solid ${palette[2]};background:#fffaf4;border-radius:10px;padding:7px 9px;font-weight:800;color:${palette[1]}">${s.button}</button>`:''}`;const btn=bar.querySelector('#lm-drive-protect-action');if(btn)btn.onclick=()=>s.kind==='off'?openSettings():reconnectAndProtect(btn)}
function tryStartupProtection(){clearTimeout(startupTimer);startupTimer=setTimeout(async()=>{const l=ledger();if(!l.dirty||!hasActiveToken()||typeof window.__lmDriveBackupNow!=='function'){render();return}try{await window.__lmDriveBackupNow('startup-pending')}catch{}finally{render()}},1800)}
window.addEventListener('lm:data-changed',e=>{setDirty(e.detail||{});tryStartupProtection()});
window.addEventListener('lm:drive-backup-complete',e=>confirmProtected(e.detail||{}));
window.addEventListener('focus',()=>{render();tryStartupProtection()});
document.addEventListener('visibilitychange',()=>{if(document.visibilityState==='visible'){render();tryStartupProtection()}});
new MutationObserver(render).observe(app,{childList:true,subtree:true});
setTimeout(()=>{render();tryStartupProtection()},1200);
window.__lmDriveProtectionStatus=protectionStatus;