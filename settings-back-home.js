document.addEventListener('click',e=>{const btn=e.target.closest('#settings-back');if(!btn)return;e.preventDefault();e.stopImmediatePropagation();btn.disabled=true;btn.setAttribute('aria-busy','true');requestAnimationFrame(()=>{const u=new URL(location.href);u.search='';u.searchParams.set('v','214');u.searchParams.set('home','1');u.searchParams.set('fresh',String(Date.now()));location.replace(u.toString())})},true);
